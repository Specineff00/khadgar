schema {
  query: Query
}

type Query {
  personalisedCompanies(offset: Int!, limit: Int!, companyName: String!): [Company!]!
}

type Company {
  name: String
  shortDescription: String
  size: CompanySize
  urlSafeName: String
}

type CompanySize {
  value: String
}
